# TODO

## Work groups

Items remain in the canonical lifecycle tables below; these ranges provide the
domain grouping without weakening the required status schema.

- **Project split and portability:** XTPU-0193–XTPU-0206, XTPU-0088
- **Hardware and storage:** XTPU-0104–XTPU-0110
- **Scheduling, network, and security:** XTPU-0111–XTPU-0114
- **Developer tools:** XTPU-0115–XTPU-0120
- **Local files and routines:** XTPU-0125
- **Vision and image processing:** XTPU-0049, XTPU-0126–XTPU-0129
- **Camera, equipment, and audio:** XTPU-0130–XTPU-0133
- **Wiring gaps:** XTPU-0183, XTPU-0185–XTPU-0190
- **Install and verification:** XTPU-0191
- **Ledger hygiene:** XTPU-0184
- **SonarCloud remediation:** XTPU-0177–XTPU-0180

## Findings

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| XTPU-0193 | open | high | s | XTPU-0194, XTPU-0195, XTPU-0196 | Decide where the boundary between the Cinnamon helper and the Python client sits, because every other row in this plan follows from it. Two shapes are coherent. **A, published model:** the helper keeps a real in-panel menu and renders a UI model the Python client publishes, sending user intents back; the panel stays the product, and the cost is a third contract between the two — today they meet at exactly two, the snapshot file and the D-Bus interface, and each needed a parity gate. **B, launcher:** the helper shrinks to a panel icon, a status label, and a launch action, and all real interaction happens in a Python-owned window; far less contract surface and immediately portable, but the in-panel menu is the thing Cinnamon users get and it would be gone. B is cheaper and more portable; A keeps the product's character. Decide before extracting anything, since A and B move different code. |
| XTPU-0194 | open | high | m | XTPU-0193, XTPU-0201, XTPU-0202 | Start the Python client: project skeleton, transport, and contract validation. The transport is the D-Bus gateway plus the snapshot file reader the applet already models; the validation is where the split pays immediately, because the applet carries about 2,727 lines of hand-written contract mirrors (`runtime-*-contract.js`, `workload-manifest.js`, the snapshot validators) that exist only because GJS has no schema engine — `generate-snapshot-contract.js` says exactly that, and the drift it guards against once made the applet reject every snapshot the runtime published. A Python client validates against the canonical schemas with `jsonschema`, the same way `registry.py` does, and omnitensor already ships `schemas/` inside its wheel — so that whole mirror layer is deleted, not ported, and the parity gates shrink to one direction (OMNI-0344 records the dependency-shape decision). Domain and workflow layers follow as XTPU-0201 and XTPU-0202. |
| XTPU-0201 | open | high | m | XTPU-0194 | Port the domain and state layer to the Python client: snapshot normalization, the workload portfolio and catalog, device aggregation, and the poller/state machine that `manager.js` owns today (`domain.js` 765 lines, `manager.js` 890). This layer has no GJS dependency and its behaviour is heavily pinned — the accelerator-preference ordering, availability-outranks-preference, and absolute-input-root rules all carry regression tests worth translating alongside the code rather than rediscovering. One constraint from the architecture review: `manager.js` also carries transport-failure text mapping (`controlFailureText`/`jobFailureText` over the `TRANSPORT_FAILURES` patterns, `:92-115`) and tab navigation (`selectTab`), which belong to the gateway layer and the view-model respectively — port the state machine without those two hitchhikers. |
| XTPU-0202 | open | high | l | XTPU-0194, XTPU-0201, XTPU-0190 | Port the workflow layer: the shared `RuntimeWorkflowController` lifecycle, the generic workflow registry, and the five bespoke controllers (event-import, document-question, selected-text, file-organizer, media-transcription). Port order matters less than scope discipline: the 14 never-wired modules (XTPU-0183) move as libraries only — deciding their application layers stays with XTPU-0200, and XTPU-0190's what-does-a-generic-run-send decision lands here rather than in GJS. The architecture review measured the duplication this port must not carry over: the `_accepted`/`_poll`/`_result` lifecycle is reimplemented in all five bespoke controllers plus the generic one — roughly 450 to 650 duplicated lines, with `document-question.js:314-392` representative — and `event-import.js` (770 lines) bypasses the shared base entirely. The Python shape is one lifecycle class plus per-workflow terminal parsers and input acquisition; event-import in particular is a rewrite onto that base, not a port. |
| XTPU-0195 | open | high | m | XTPU-0193 | Define and gate the contract the helper renders, if XTPU-0193 chooses the published-model shape. It needs a UI model document — sections, rows, controls, status, and their enabled state — and an intent channel carrying what the user pressed, both versioned and both schema-validated, because a field one side adds and the other does not know invalidates the whole document. That failure mode is not hypothetical here: `docs/installation.md` already records it for the snapshot, the plug-in inventory hit it this week, and the fix each time was to deploy the reader first. Gate it the way the existing two are gated, with a parity check that runs wherever both checkouts are present, and give the helper a forward-compatible reader so an unknown section is skipped rather than blanking the panel. |
| XTPU-0196 | open | medium | l | XTPU-0193, XTPU-0194 | Shrink `cinnamon-xpuwlm` to the helper it becomes, and delete rather than keep what moved. What remains is the panel presence (icon, label, tooltip, accessibility), the popup shell, the renderer for whatever XTPU-0193 settles on, the Cinnamon-specific ports that cannot move (GTK choosers, clipboard, notifications, settings binding, xlet lifecycle), and packaging for the Spices. Everything else goes. Two things to preserve on the way out: the reachability gate in `tests/contract/production-payload-contract.test.js`, which is what proves a module is actually shipped rather than merely present, and the root-shim contract that makes nested CommonJS imports work under Cinnamon. Both should shrink with the tree rather than be dropped. |
| XTPU-0197 | open | high | m | XTPU-0194 | Define the Python client's platform ports as interfaces and ship the Linux implementations. Eight ports exist today and every one is Linux-shaped: file and folder choosers, clipboard reading, screenshot capture, notifications, autostart, the state directory, path handling, and the transport. Each becomes an interface with a Linux implementation, plus a deliberate refusal for a port a platform genuinely lacks rather than a silent no-op — so Windows (XTPU-0203) and macOS (XTPU-0204) are additions rather than rewrites. The transport port is decided and already live on Linux: framed msgpack over a unix-domain socket (a per-user named pipe on Windows), one codec with no negotiation, by operator decision recorded in omnitensor's `scripts/benchmark-transport-codecs.py` docstring; the runtime serves it (`socket_transport.py`) and this applet already speaks it (`lib/cinnamon-socket-adapter.js` + `lib/msgpack-codec.js`), with session D-Bus removed outright on both sides. The Python client's port is therefore a port of working code, not a design: connect, length-prefix frame, request/reply envelope (`control-request`/`control-reply` schemas), liveness from the socket file's presence under the service's `RuntimeDirectory`. |
| XTPU-0198 | open | medium | m | XTPU-0194, XTPU-0197 | Package and install the three pieces per platform. Today one `package-applet.js` stages a checksummed Cinnamon payload and `docs/installation.md` describes a single Linux sequence. After the split there are three artefacts with an ordering constraint the docs already state for a different reason: the reader deploys before the writer, so the helper updates before the client publishes a model it does not understand. Cover the Cinnamon Spice, the Python client on each platform, and the runtime service, and keep the install-time verification that names a mismatch rather than leaving a desktop that silently shows nothing. |
| XTPU-0199 | open | medium | m | XTPU-0205 | Unwrap the translation calls and ship English only. `_()` and `N_()` are identity functions whose 742 and 163 call sites across thirty modules unwrap mechanically, but `format()` with 113 call sites and `ngettext()` with 34 do real work — positional interpolation and plural selection — so they keep a plain non-gettext helper rather than being deleted, or plurals silently become "1 items". Sequence against the split: a string about to move to Python is cheaper unwrapped once, on the side that ends up owning it, so modules staying in the helper can be swept now and moving modules swept as they land. Infrastructure removal is XTPU-0205 and only makes sense once no module imports the port. |
| XTPU-0200 | open | medium | s | XTPU-0193, XTPU-0194 | Re-decide the unwired modules in light of the split, because it changes several answers. XTPU-0186 asks where `artifact-qualification` and `readiness-acceptance` get their documents — in a Python client they sit next to the runtime that already produces them, and the two-repository contract change it describes may not be needed at all. XTPU-0187 asks what `runtime-control-service` is for, given it duplicates `omnitensor/src/omnitensor/control.py` — a Python client makes deleting it obviously right rather than arguable. XTPU-0189's file benchmarks and XTPU-0188's routine collector both want a host runtime the Python side has and GJS does not. Settle these as part of the split rather than separately, and close the ones the split makes moot. |
| XTPU-0206 | open | medium | s | XTPU-0194, XTPU-0196 | Extract the two model-free layers still living inside the `MenuView` class, because they are exactly what the helper keeps under either split shape, so the work is not thrown away. The class carries about eighty methods across five responsibilities; the two that lift cleanly are the widget factory with its accessibility helpers (`menu-view.js:1376-1533`, ~158 lines) and the focus-and-draft preservation logic (`:427-472` plus its call sites, ~152 lines) — the sibling modules `menu-actor-utils.js` and `menu-focus.js` already hold the stateless halves of each, so this completes an extraction that was started rather than inventing a new boundary. Doing it before the split makes the XTPU-0194/XTPU-0196 line crisper: what remains in the class afterwards is per-screen rendering and detail routing, which is precisely the part XTPU-0193 decides the fate of. |
| XTPU-0205 | open | low | s | XTPU-0199 | Delete the translation infrastructure once XTPU-0199 leaves it without importers: `lib/i18n.js` and its root shim, `po/` and the shipped `.pot`, `scripts/generate-pot.js` with its `generate:pot` script and the template-lockstep contract test, the gettext binding in `applet.js`, and the `.pot` entries in `validate-artifacts.js` and the packaging inventory. The per-function coverage and payload-reachability gates both track these files, so they shrink in the same change rather than failing afterwards. |
| XTPU-0190 | open | high | m | XTPU-0183 | Decide what a generic workflow run sends, because it currently cannot send anything. Every job the runtime accepts carries input tensors — `dispatch.py` refuses a payload whose `inputs` is not an array — so a generic run refuses locally with "This workflow has no input to send yet" rather than spending a round trip to be refused. Live on this host `visual-library` reads Ready and its Run control is enabled, so the refusal is the only thing between the user and a working run. The applet already stages inputs for the picture flow through `job-submission.js` and the snapshot names which profiles have them (`inputs.runnable`), so the mechanism exists; what is missing is the product decision about which input a generic run uses — the whole listed set, one the user picks, or the same selection the picture flow made — and where that choice appears in a surface whose entire interaction today is one Run button. |
| XTPU-0191 | open | low | s | — | Live verification races the service it verifies. `omnitensor-verify-install` run three seconds after `systemctl --user restart` reported `dbus: ApplyCommand raised TimeoutError` while the service was answering normally; the probe simply started before the bus name was claimed and retried for its full thirty seconds. The message now names the underlying cause, but the check still spends the whole budget before reporting. Consider waiting for the name to appear — `dbus-monitor` on `NameOwnerChanged`, or systemd's own readiness — instead of polling blind, so a restart-then-verify sequence is fast and quiet rather than slow and alarming. |
| XTPU-0185 | open | medium | l | XTPU-0183 | Wire the caption and presentation chain, which needs one decision first: where the deterministic write path confirms. `caption-export` turns a completed media transcription into SRT/WebVTT and `presentation-planning` exports a deck, and both reach the user's filesystem through `deterministic-action-port` — preview, conflict check, audit evidence, and a fresh single-use confirmation before the writer runs. The applet has no confirmation surface today, and inventing one silently would put a file-writing dialog in a panel menu on its own authority. Decide whether the confirmation lives in the popup, a modal, or the runtime, then wire `caption-export` behind the existing media workflow's completed result, and `presentation-review` → `presentation-planning` → `rehearsal-briefing` behind a presentation workflow. |
| XTPU-0186 | open | medium | m | XTPU-0183 | Decide where `artifact-qualification` and `readiness-acceptance` get their documents. Both validate records the applet never receives: the D-Bus plug-in inventory carries `{id, version, format, ready, reason}` per artifact and no qualification envelope, and no method publishes readiness evidence. Either extend the runtime's inventory reply to carry them — a coordinated two-repository contract change — or accept that these validate build-time artifacts only and belong in `scripts/`, not in the shipped applet. |
| XTPU-0187 | open | medium | s | XTPU-0183 | Decide what `runtime-control-service` is for. It implements the runtime side of the version 2 control contract — policy repository, revision guard, portfolio serialization — which `omnitensor/src/omnitensor/control.py` already owns and which the applet reaches through `runtime-control-gateway.js`. Wiring it would make the applet a second policy authority against an architecture where the runtime is authoritative. Either keep it as an explicitly named reference implementation exercised only by tests, and record that here, or delete it. |
| XTPU-0188 | open | medium | l | XTPU-0183 | Wire `screenshot-assistant` and `routine-recognition`, both of which need a source the applet does not collect. The screenshot assistant accepts one explicitly captured screenshot or selected image and submits it to the media vision worker: it needs a Cinnamon screenshot capture port alongside the existing image chooser. Routine recognition accepts bounded, content-free activity summaries — time buckets, weekday, categorical application and device classes — which nothing produces today; it needs a consented collector with the same redaction discipline as `telemetry-recorder`, and its consent is a separate decision from the load record's. |
| XTPU-0189 | open | medium | l | XTPU-0183 | Decide what the file workflows compare before wiring them. `file-categorization`, `image-duplicate-benchmark`, and the shared `file-auto-tagging` are not classifiers — they are benchmark harnesses that measure candidate implementations against each other and recommend one, and `validCandidates` refuses fewer than two. So wiring them needs two real taggers, not a controller: `familyOf` gives a deterministic suffix-based host candidate for free, and the second would be a GPU candidate through the runtime. That is now reachable — the external workers start and every tool reads Ready since the launch fix — so the blocker is no longer availability but meaning: two host candidates would also run, and either way it means choosing what the applet's tagging does and against which taxonomy. Selection itself is solved through `document-source-port`; the open question is what is being compared. |
| XTPU-0183 | in_progress | medium | l | XTPU-0193, XTPU-0200, XTPU-0185, XTPU-0186, XTPU-0187, XTPU-0188, XTPU-0189 | **Paused pending XTPU-0193.** Wiring these into the GJS applet now would build application layers the split then moves or deletes: under either split shape the portable modules go to the Python client unwired and receive their controllers there, so continuing here is work performed twice. The five decision rows (XTPU-0185–0189) stay open because their questions survive the move; the GJS implementation half of each does not. Original scope, kept for when the split lands or is rejected — wire the remaining 14 shipped `lib/` modules `applet.js` still cannot reach: `artifact-qualification`, `caption-export`, `deterministic-action-port`, `file-auto-tagging`, `file-categorization`, `image-duplicate-benchmark`, `presentation-planning`, `presentation-review`, `readiness-acceptance`, `rehearsal-briefing`, `routine-recognition`, `runtime-control-service`, `screenshot-assistant`, `workload-benchmark`. They are domain and policy libraries with no application layer, so each needs a controller and a view-model, not a require. The generic workflow surface is now wired and is the pattern to follow: `lib/generic-workflow-controller.js` drives one runtime-backed profile through `RuntimeWorkflowController`, `workflow-wiring.js` projects a definition from each executable bundled manifest, and `menu-view.js` renders it behind a `generic:<id>` detail — a workflow whose whole interaction is run, watch, read the evidence needs no bespoke module. Modules that own their own interaction (`caption-export` and `presentation-planning` write files through `deterministic-action-port`; `screenshot-assistant` and `routine-recognition` take explicit input) need bespoke controllers instead. Packaging tracks reachability: `tests/contract/production-payload-contract.test.js` lists what stays unshipped and shrinks as each is wired. |
| XTPU-0184 | open | low | s | — | Decide what `related ids` means once a referenced row is removed, then repair the ledger. 29 of 55 rows cite at least one ID that no longer has a row; the prerequisite cluster XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0099, XTPU-0101 alone is cited by 25 of them. Cross-repository `OMNI-####` references are deliberate and are not affected. Either restore the prerequisite rows the dependencies point at, or rewrite each dependency as prose in its description. |

## Blocked

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| XTPU-0088 | blocked | high | xl | XTPU-0197, XTPU-0203 | Full Windows inference parity is blocked on a compatible OmniTensor Windows service: the current control plane is a Linux unix-domain socket, fallback discovery reads `/dev` and `/sys`, and accelerator SDK/driver support must be proven. The transport half is decided and shipped on Linux — framed msgpack over a unix socket, a per-user named pipe being the Windows equivalent — which leaves this row blocked on the genuinely Windows-shaped work: service-authoritative device discovery to replace the `/dev` and `/sys` reads, accelerator SDK/driver proof, and a Windows host to develop against before the planned Python client (XTPU-0194, XTPU-0203) can provide full functionality there. |
| XTPU-0203 | blocked | high | l | XTPU-0197, XTPU-0088 | Windows implementations of the client's platform ports. — blocked twice over: no Windows host exists here to develop or verify against, and the runtime side has no Windows service or per-user IPC transport (XTPU-0088), so a Windows client would have nothing to talk to. Unblock with a Windows development host plus the runtime transport work. |
| XTPU-0204 | blocked | medium | l | XTPU-0197 | macOS implementations of the client's platform ports. — blocked: no macOS host exists here to develop or verify against, and the runtime has never been built or probed on macOS (device discovery reads `/dev/dri`, `/dev/accel`, `/dev/apex_*`). Unblock with a macOS development host and a runtime discovery story for Metal-class hardware. |
| XTPU-0104 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0098, XTPU-0099, XTPU-0101, OMNI-0085, OMNI-0086, OMNI-0087 | Fan degradation prediction is blocked on representative failing/normal fan data, device-specific RPM/PWM/load telemetry, an acoustic or vibration fixture if those signals are claimed, a signed model, and real failure acceptance; NASA aircraft-engine simulation data cannot validate desktop fans. |
| XTPU-0105 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0085, OMNI-0086, OMNI-0087 | Power-supply health scoring is blocked because this host exposes no trustworthy PSU voltage/current/temperature/fan telemetry; obtain supported PMBus/BMC/vendor hardware, labeled failure data, and a signed qualified model. |
| XTPU-0106 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0085, OMNI-0086, OMNI-0087 | UPS battery degradation is blocked because no UPS/power-supply device or Network UPS Tools client exists here; obtain supported hardware, discharge/load histories, failure labels, and a signed qualified model while preserving deterministic shutdown policy. |
| XTPU-0107 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0088, OMNI-0089, OMNI-0090 | Disk/SSD failure warning is blocked on permission to read all seven discovered SMART/NVMe devices, representative local SSD/HDD histories and outcomes, device-family validation, signing identity, and real-drive false-positive/recovery acceptance. |
| XTPU-0108 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0085, OMNI-0086, OMNI-0087 | Memory-fault precursor detection is blocked because EDAC reports no memory controller on this host and representative corrected-error/failure sequences are absent; obtain a supported ECC/RAS host, labels, signed model, and real acceptance. |
| XTPU-0109 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0103, XTPU-0107, OMNI-0088, OMNI-0089, OMNI-0090 | Storage-tier recommendation is blocked on the storage telemetry/model pipeline, representative access/retention histories, measured utility, and safe deterministic migration/rollback adapters; model output must remain advisory. |
| XTPU-0110 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0091, OMNI-0092, OMNI-0093 | Workload placement hints are blocked on representative local job traces and target utility/fairness labels, a signed qualified model, deterministic starvation policy, and named-host acceptance; public cluster traces are prototype data only. |
| XTPU-0111 | blocked | medium | l | XTPU-0095, XTPU-0099, XTPU-0100, OMNI-0091, OMNI-0092, OMNI-0093 | Queue-delay forecasting is blocked because the runtime has no labeled representative workload traces, qualified signed `resource-scheduler` model/tensor contract, bounded advisory pipeline, or named-hardware accuracy, calibration, latency, and fairness evidence. Unblock by publishing those runtime artifacts, freezing a local trace corpus with target delays, and benchmarking deterministic host, GPU, and hybrid paths before exposing a typed advisory through the generic Cinnamon surface. |
| XTPU-0112 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0098, XTPU-0101, OMNI-0108, OMNI-0109, OMNI-0110, OMNI-0111, OMNI-0184 | Port-scan/reconnaissance detection is blocked on a least-privilege flow collector, BPF access, privacy-reviewed current traffic labels, signed model, false-positive/attack-recall gates, and real network acceptance. |
| XTPU-0113 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0108, OMNI-0109, OMNI-0110, OMNI-0111 | Wi-Fi interference classification is blocked because Wi-Fi is disconnected and `iw` is absent; obtain survey/retry/channel telemetry, controlled congestion/interference/hardware-fault labels, a signed model, and real radio acceptance. |
| XTPU-0114 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0098, XTPU-0101, XTPU-0103, OMNI-0184 | Privilege-escalation precursor detection is blocked on approval of a separate host-security profile, missing audit tools and BPF privilege, a least-privilege redacted event helper, representative attack/normal sequences, and review-only acceptance. |
| XTPU-0115 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099, OMNI-0094, OMNI-0095, OMNI-0096 | Test prioritization experiments are blocked on a provenance-safe labeled change/failure/duration/ownership/coverage corpus, a qualified signed `build-advisor` model/tensor contract, its ranking pipeline, and named-hardware acceptance. Unblock by freezing the corpus, mandatory-test allowlist, time-to-first-failure and recall-at-budget metrics, publishing the runtime artifacts, and benchmarking deterministic, host, GPU, and hybrid rankers without omitting required checks. |
| XTPU-0116 | blocked | medium | l | XTPU-0095, XTPU-0099, XTPU-0101 | API-misuse classification is blocked because no license-compatible labeled corpus, frozen misuse taxonomy/splits, qualified signed model, or acceptance thresholds exist. Unblock by selecting one language/API corpus with gold labels, defining precision, recall, false-positive, latency, memory, and explanation-quality gates, then benchmarking deterministic AST/rules, host, GPU, and hybrid candidates on identical held-out data and named hardware. |
| XTPU-0117 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099 | Fuzz-input prioritization is blocked on a selected native fuzzer/target set, reproducible seeded corpora, instrumented path and held-out bug ground truth, an energy measurement method, and any qualified signed learned scorer. Unblock by freezing those fixtures and paths/bugs-per-wall-clock-and-joule metrics, then running repeated native-selection, host, GPU, and hybrid campaigns with identical budgets on named hardware. |
| XTPU-0118 | blocked | medium | l | XTPU-0095, XTPU-0099 | Fuzz-crash deduplication is blocked on a license-compatible crash corpus with reviewed bug-equivalence clusters across stack/sanitizer variants, a frozen normalization baseline, qualified signed embedding artifacts, and an operator-cost protocol. Unblock by publishing those fixtures and pairwise precision/recall, latency, peak-memory, and review-time metrics, then benchmarking normalized signatures, host clustering, GPU embeddings, and hybrid cascades on identical named hardware. |
| XTPU-0119 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0103 | Compiler-option suggestion is blocked on pinned compiler/toolchain targets, validated allowlisted profiles, a reproducible labeled build corpus, correctness oracles, and any qualified signed advisor. Unblock by freezing those inputs and deterministic refusal policy, then comparing search, host, GPU, and hybrid recommenders on identical builds for correctness, build cost, output size, runtime, and peak memory without generating arbitrary flags. |
| XTPU-0120 | blocked | medium | l | XTPU-0095, XTPU-0096, XTPU-0099, OMNI-0094, OMNI-0095, OMNI-0096 | Resource-regression experiments are blocked on versioned known-good/regressed builds, controlled repeated CPU, memory, I/O, GPU, and startup measurements, noise budgets and labels, plus qualified signed `build-advisor` artifacts and acceptance. Unblock by freezing the workload/host harness and false-positive/detection gates, publishing the runtime model and pipeline, and comparing statistical, host-model, GPU-model, and hybrid detection on identical held-out versions. |
| XTPU-0125 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099, XTPU-0100, XTPU-0101, OMNI-0097, OMNI-0098, OMNI-0101 | Visual/semantic image relatedness is blocked on a frozen retrieval corpus and metrics, signed CLIP-compatible ncnn artifact, embedding consumer, batching crossover measurements, and named-GPU acceptance. |
| XTPU-0126 | blocked | high | xl | XTPU-0095, XTPU-0097, XTPU-0099, XTPU-0101 | PCB inspection is blocked on representative boards and missing/misplaced/rotated/damaged-component labels, a signed detector, camera/lighting fixture, and real-board acceptance; DeepPCB surface-defect pairs do not cover the requested component task. |
| XTPU-0127 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099, XTPU-0102 | Foreground/person segmentation is blocked on selecting a license-compatible model, a frozen labeled mask corpus and quality thresholds, a publisher signing identity, an exported qualified ncnn artifact, and named-Vulkan accuracy and performance evidence; this host provides suitable AMD Vulkan hardware but none of the model evidence. |
| XTPU-0128 | blocked | medium | m | XTPU-0100, XTPU-0102, XTPU-0103, XTPU-0127 | Background removal is blocked on XTPU-0127's absent qualified mask producer and original-geometry contract; after that exists, measure host/GPU composition placement and verify atomic no-overwrite PNG/WebP publication against real masks. |
| XTPU-0049 | blocked | low | m | XTPU-0129, OMNI-0105, OMNI-0106 | The profile now declares no model at all, because the runtime refuses one without a sha256 and the ncnn artifact has never been produced; restore the declaration from `omnitensor/model-recipes/retinexformer-lol-v1.json` together with the artifact and its digest. Then record reproducible `low-light-enhancement` 0.2.0 acceptance on the named RX 6600 XT: exact signed ncnn artifact/runtime identity, full Vulkan execution with no CPU fallback, portable/native and paired-image fidelity, warm p95, and measured end-to-end speedup over the documented CPU baseline. The profile remains disabled until evidence exists. |
| XTPU-0129 | blocked | high | xl | XTPU-0049, XTPU-0095, XTPU-0097, XTPU-0101, OMNI-0048, OMNI-0102, OMNI-0103, OMNI-0104, OMNI-0105, OMNI-0106 | Low-light enhancement 0.2.0 is Vulkan/ncnn-only and returns enhanced RGB. Operational readiness remains blocked on the signed Retinexformer artifact, licensed paired corpus, completed pipeline, Cinnamon image-selection/run/progress/result UX, fidelity evidence, and named-GPU end-to-end acceptance. |
| XTPU-0130 | blocked | medium | xl | XTPU-0095, XTPU-0097, XTPU-0098, XTPU-0100, XTPU-0101 | Ergonomic posture reminders are blocked because this host has no webcam; obtain consented representative posture data, a camera fixture, signed pose artifact, non-medical thresholds, privacy controls, and real acceptance. |
| XTPU-0131 | blocked | medium | l | XTPU-0100, XTPU-0101, XTPU-0103, XTPU-0130 | Webcam auto-framing is blocked on the pose/person provider, absent webcam, camera-control adapter, smoothing/crop benchmarks, preview/confirmation UX, and real-device acceptance. |
| XTPU-0132 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0098, XTPU-0101 | 3D-printer failure detection is blocked on a printer/camera fixture, printer-specific normal/failure corpus, signed model, continuous-monitoring consent/scheduling, and false-positive acceptance; model output must not stop a print. |
| XTPU-0133 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099, XTPU-0102 | Music genre, instrument, and mood tagging is blocked on separate bounded label ontologies, license-compatible timestamped audio corpora and held-out splits, qualified signed audio artifacts/tensor contracts, confidence calibration, and named-hardware evidence. Unblock by publishing those inputs and a timestamped evidence schema, then benchmarking deterministic features and host/GPU candidates for per-label precision/recall, calibration, latency, and peak memory on explicitly selected audio. |

## Rejected / Won't fix

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| XTPU-0087 | rejected | high | l | XTPU-0193 | An Electron/Node tray spike was the recorded Windows path, premised on reusing the applet's JavaScript library surface. The split plan (XTPU-0193–XTPU-0205) supersedes that premise: the portable client will be Python, so the JavaScript-reuse argument and the Electron spike no longer describe the intended future. Windows client work now lives in XTPU-0203 (platform ports) and the runtime-side transport it needs stays recorded in XTPU-0088. Kept as a tombstone so a later audit does not re-propose the Electron path. |
| XTPU-0177 | wont_fix | high | xs | — | Sonar CSS rule S4654 reports 24 uses of `spacing` as an unknown browser property, but the shipped stylesheet targets Cinnamon's St theme engine where `spacing` is a supported layout property; retain the declarations and mark the Sonar issues false-positive when project administration is available. |
| XTPU-0178 | wont_fix | high | xs | XTPU-0175 | Sonar S5443 reports two `/tmp` strings that are inert test-fixture data and never reach a filesystem API; S1523 reports the intentional CJS compatibility loader, whose dynamic compilation is restricted to the enumerated checked-in production sources. Retain these three reviewed patterns and mark the Sonar issues false-positive when project administration is available. |
| XTPU-0179 | wont_fix | medium | xs | XTPU-0176 | Retain the reviewed JavaScript compatibility and design patterns behind Sonar S7781, S7784, S7757, and S2094: global regular-expression replacement is supported across the Cinnamon 6.0–6.6 floor; `structuredClone` is absent from the Cinnamon 6.6 CJS runtime and the JSON-only contracts intentionally reject unsupported values; constructor assignments make per-instance cancellation state explicit; and the empty workflow class is a descriptor-assembly surface. Mark these 19 issues won't-fix after the next scan. |
| XTPU-0180 | wont_fix | medium | xs | XTPU-0176 | Sonar S7924 evaluates 16 translucent prototype backgrounds without their opaque dark parent surfaces and treats eight `aria-hidden` SVG profile icons as text. Their actual composited contrast ranges from 5.77:1 to 13.49:1; retain those rules and mark them false-positive after the next scan. The separate primary-button finding was applicable and its gradient now meets 4.5:1 at both stops. |
