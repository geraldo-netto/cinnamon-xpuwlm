# TODO

## Open

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |

## Blocked / Deferred

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| XTPU-0274 | blocked | medium | s | Decide whether `panelModel` treats every unresolved alert as attention or only `warning` and `critical` alerts. `unresolvedAlerts` ignores severity, so advisories currently outrank paused and running states. This cross-repository choice also depends on whether alerts are acknowledged or expire, tracked by OmniTensor OMNI-0554. |
| XTPU-0272 | blocked | low | xs | Decide whether `verifyInstall` audits only world-writable entries, rejects any mode more permissive than release modes 0644/0755, or reports mode drift as a non-failing advisory. Stricter choices change the definition of a clean install. |
| XTPU-0273 | blocked | low | xs | Decide whether `STALE_AFTER_MS` remains 15 seconds or derives from the runtime's 10-second idle cadence, such as 20 seconds. Current margin is five seconds; increasing it reduces false stale reports but delays true stopped-runtime reports. |
| XTPU-0259 | blocked | low | xs | The mutation campaign's scope is an enumeration: `BEHAVIOR_MODULES` in `scripts/mutation-plan.js` names three of the six `lib/` modules by hand and `tests/unit/mutation-config.test.js` pins the count at three, so `panel-icon`, `settings-window-placer` and `window-placement` are mutated by nothing and nothing says so. Blocked: mutation testing is the maintainer's, written and run by hand, so widening its scope is their call and not one this assistant may make. |
| XTPU-0197 | blocked | medium | s | Complete the Python client's remaining platform ports: clipboard, screenshot, notifications, and autostart. All remaining work belongs to `../xpuwlm` and depends on the product decisions in XTPU-0247 and XTPU-0248. |
| XTPU-0247 | blocked | high | s | Write the Linux implementations of the two capture ports declared with none — `Clipboard` and `Screenshot` in `../xpuwlm/src/xpuwlm/ports.py`, which today answer through `missing()` and raise `UnsupportedPort`. Screenshot capture is also the source XTPU-0188 needs. Blocked on the maintainer: neither port has a caller, so implementing them writes host capture code for a feature nobody has asked for yet; decide whether the capture surface is wanted before the implementation exists. |
| XTPU-0248 | blocked | medium | s | Write the Linux implementations of the two session ports declared with none — `Notifications` and `Autostart` in `../xpuwlm/src/xpuwlm/ports.py`, which today raise `UnsupportedPort`. Blocked on the maintainer, for the same reason as XTPU-0247: neither port has a caller, and autostart in particular is a decision about what the client does to the user's session rather than a missing implementation. |
| XTPU-0222 | blocked | low | xs | Verify on a live Cinnamon desktop that the panel icon remains inside panel heights 20, 28, and 40 pixels and redraws after height changes. Automated geometry coverage exists, but visual acceptance needs a physical desktop session. |
| XTPU-0244 | blocked | low | xs | Decide whether a snapshot with no usable timestamp can be reported as connected. `stateFromDocument` reads `generatedAt` only when it is an integer and `isStale` skips the check when it is null, which `tests/unit/snapshot-reader.test.js` pins deliberately ("a snapshot with no timestamp is read rather than judged stale"). The consequence is that a file left behind by a runtime that stopped — one written once, or written with a timestamp the panel cannot read — is drawn as a live runtime for the rest of the session, which is the one picture staleness exists to prevent. The canonical schema declares `generatedAt`, so the alternative is to treat its absence as a document this panel cannot judge and say so. Blocked on the maintainer: is a snapshot whose freshness cannot be established still `connected`, or is it `stale` (or `malformed`)? Either answer changes shipped behaviour and the pinned test. |
| XTPU-0200 | blocked | medium | s | Decide whether XTPU-0186, XTPU-0188, and XTPU-0189 remain in this applet ledger or move to `../xpuwlm`, where their product documents and implementation surface now live. |
| XTPU-0183 | blocked | medium | l | Decide which former unwired applet workflows the Python client should carry, then implement those retained. Blocked until XTPU-0185, XTPU-0186, XTPU-0188, and XTPU-0189 settle their product requirements. |
| XTPU-0185 | blocked | medium | l | Decide whether caption and presentation file-write confirmation lives in the client workload window, a modal, or the runtime. Implementation also requires OmniTensor manifests for those workloads before the client can wire preview, conflict checks, audit evidence, and single-use confirmation. |
| XTPU-0186 | blocked | medium | m | Decide whether qualification and readiness evidence belongs in OmniTensor's `describe-plugins` contract or remains build-time validation under `../omnitensor/scripts/`. Extending the inventory requires a coordinated contract change; keeping it build-time removes any shipped client role. |
| XTPU-0188 | blocked | medium | l | Give `screenshot-assistant` and `routine-recognition` the sources neither client collects. The screenshot assistant accepts one explicitly captured screenshot or selected image and submits it to the media vision worker; `../xpuwlm` has a file chooser (`ui/fields.py:177`) but no screenshot capture at all, which is also the missing port XTPU-0197 tracks. Routine recognition accepts bounded, content-free activity summaries — time buckets, weekday, categorical application and device classes — which nothing produces on either side; it needs a consented collector with the runtime telemetry recorder's redaction discipline, and its consent is a separate decision from the load record's. Neither workload is declared in `../omnitensor/plugin-manifests/` today, so the runtime half is a prerequisite. Blocked on the maintainer: screenshot capture and a consented activity collector are both new data sources with their own consent decisions, and neither workload is declared in `../omnitensor/plugin-manifests/`. |
| XTPU-0189 | blocked | medium | l | Define file-tagging semantics and taxonomy before implementing deterministic host and GPU candidates. OmniTensor also needs corresponding workload declarations before the client can compare candidates. |
| XTPU-0218 | blocked | low | xs | Decide the panel's poll cadence against the cadence the service actually publishes at. Revealed by resolving XTPU-0207: the shipped default is one second, while `../omnitensor/src/omnitensor/service.py:151-161` (the loop itself now lives in `publish_loop.py`) publishes every two seconds under load and every ten when idle, with a ten-second proof-of-life heartbeat. So at the default the panel performs one blocking read and JSON parse per second to observe a document that changes at most half that often, and a tenth as often on an idle desk. Options: (a) leave one second and accept the reads, (b) follow the service's two-second publish interval, (c) track it — poll fast only while the last snapshot said something was running. (b) and (c) change shipped behaviour and the regression test that pins it (`tests/regression/refresh-interval-default-regression.test.js`), so the choice is the maintainer's. |
| XTPU-0088 | blocked | high | xl | Full Windows inference parity is blocked on a compatible OmniTensor Windows service: the current control plane is a Linux unix-domain socket, fallback discovery reads `/dev` and `/sys`, and accelerator SDK/driver support must be proven. The transport half is decided and shipped on Linux — framed msgpack over a unix socket, a per-user named pipe being the Windows equivalent — which leaves this row blocked on the genuinely Windows-shaped work: service-authoritative device discovery to replace the `/dev` and `/sys` reads, accelerator SDK/driver proof, and a Windows host to develop against before the Python client in `../xpuwlm` (XTPU-0203) can provide full functionality there. |
| XTPU-0203 | blocked | high | l | Windows implementations of the client's platform ports. The client itself now exists (`../xpuwlm`), so this is no longer waiting on a codebase — but it is still blocked twice over: no Windows host exists here to develop or verify against, and the runtime side has no Windows service or per-user IPC transport (XTPU-0088), so a Windows client would have nothing to talk to. The ports it would implement are not yet interfaces either (XTPU-0197). Unblock with a Windows development host plus the runtime transport work. |
| XTPU-0204 | blocked | medium | l | macOS implementations of the client's platform ports. — blocked: no macOS host exists here to develop or verify against, and the runtime has never been built or probed on macOS (device discovery reads `/dev/dri`, `/dev/accel`, `/dev/apex_*`). Unblock with a macOS development host and a runtime discovery story for Metal-class hardware. |
| XTPU-0104 | blocked | high | xl | Fan degradation prediction is blocked on representative failing/normal fan data, device-specific RPM/PWM/load telemetry, an acoustic or vibration fixture if those signals are claimed, a signed model, and real failure acceptance; NASA aircraft-engine simulation data cannot validate desktop fans. |
| XTPU-0105 | blocked | high | xl | Power-supply health scoring is blocked because this host exposes no trustworthy PSU voltage/current/temperature/fan telemetry; obtain supported PMBus/BMC/vendor hardware, labeled failure data, and a signed qualified model. |
| XTPU-0106 | blocked | high | xl | UPS battery degradation is blocked because no UPS/power-supply device or Network UPS Tools client exists here; obtain supported hardware, discharge/load histories, failure labels, and a signed qualified model while preserving deterministic shutdown policy. |
| XTPU-0107 | blocked | high | xl | Disk/SSD failure warning is blocked on permission to read all seven discovered SMART/NVMe devices, representative local SSD/HDD histories and outcomes, device-family validation, signing identity, and real-drive false-positive/recovery acceptance. |
| XTPU-0108 | blocked | high | xl | Memory-fault precursor detection is blocked because EDAC reports no memory controller on this host and representative corrected-error/failure sequences are absent; obtain a supported ECC/RAS host, labels, signed model, and real acceptance. |
| XTPU-0109 | blocked | high | xl | Storage-tier recommendation is blocked on the storage telemetry/model pipeline, representative access/retention histories, measured utility, and safe deterministic migration/rollback adapters; model output must remain advisory. |
| XTPU-0110 | blocked | high | xl | Workload placement hints are blocked on representative local job traces and target utility/fairness labels, a signed qualified model, deterministic starvation policy, and named-host acceptance; public cluster traces are prototype data only. |
| XTPU-0111 | blocked | medium | l | Queue-delay forecasting is blocked because the runtime has no labeled representative workload traces, qualified signed `resource-scheduler` model/tensor contract, bounded advisory pipeline, or named-hardware accuracy, calibration, latency, and fairness evidence. Unblock by publishing those runtime artifacts, freezing a local trace corpus with target delays, and benchmarking deterministic host, GPU, and hybrid paths before exposing a typed advisory through the generic Cinnamon surface. |
| XTPU-0112 | blocked | high | xl | Port-scan/reconnaissance detection is blocked on a least-privilege flow collector, BPF access, privacy-reviewed current traffic labels, signed model, false-positive/attack-recall gates, and real network acceptance. |
| XTPU-0113 | blocked | high | xl | Wi-Fi interference classification is blocked because Wi-Fi is disconnected and `iw` is absent; obtain survey/retry/channel telemetry, controlled congestion/interference/hardware-fault labels, a signed model, and real radio acceptance. |
| XTPU-0114 | blocked | high | xl | Privilege-escalation precursor detection is blocked on approval of a separate host-security profile, missing audit tools and BPF privilege, a least-privilege redacted event helper, representative attack/normal sequences, and review-only acceptance. |
| XTPU-0115 | blocked | medium | l | Test prioritization experiments are blocked on a provenance-safe labeled change/failure/duration/ownership/coverage corpus, a qualified signed `build-advisor` model/tensor contract, its ranking pipeline, and named-hardware acceptance. Unblock by freezing the corpus, mandatory-test allowlist, time-to-first-failure and recall-at-budget metrics, publishing the runtime artifacts, and benchmarking deterministic, host, GPU, and hybrid rankers without omitting required checks. |
| XTPU-0116 | blocked | medium | l | API-misuse classification is blocked because no license-compatible labeled corpus, frozen misuse taxonomy/splits, qualified signed model, or acceptance thresholds exist. Unblock by selecting one language/API corpus with gold labels, defining precision, recall, false-positive, latency, memory, and explanation-quality gates, then benchmarking deterministic AST/rules, host, GPU, and hybrid candidates on identical held-out data and named hardware. |
| XTPU-0117 | blocked | medium | l | Fuzz-input prioritization is blocked on a selected native fuzzer/target set, reproducible seeded corpora, instrumented path and held-out bug ground truth, an energy measurement method, and any qualified signed learned scorer. Unblock by freezing those fixtures and paths/bugs-per-wall-clock-and-joule metrics, then running repeated native-selection, host, GPU, and hybrid campaigns with identical budgets on named hardware. |
| XTPU-0118 | blocked | medium | l | Fuzz-crash deduplication is blocked on a license-compatible crash corpus with reviewed bug-equivalence clusters across stack/sanitizer variants, a frozen normalization baseline, qualified signed embedding artifacts, and an operator-cost protocol. Unblock by publishing those fixtures and pairwise precision/recall, latency, peak-memory, and review-time metrics, then benchmarking normalized signatures, host clustering, GPU embeddings, and hybrid cascades on identical named hardware. |
| XTPU-0119 | blocked | medium | l | Compiler-option suggestion is blocked on pinned compiler/toolchain targets, validated allowlisted profiles, a reproducible labeled build corpus, correctness oracles, and any qualified signed advisor. Unblock by freezing those inputs and deterministic refusal policy, then comparing search, host, GPU, and hybrid recommenders on identical builds for correctness, build cost, output size, runtime, and peak memory without generating arbitrary flags. |
| XTPU-0120 | blocked | medium | l | Resource-regression experiments are blocked on versioned known-good/regressed builds, controlled repeated CPU, memory, I/O, GPU, and startup measurements, noise budgets and labels, plus qualified signed `build-advisor` artifacts and acceptance. Unblock by freezing the workload/host harness and false-positive/detection gates, publishing the runtime model and pipeline, and comparing statistical, host-model, GPU-model, and hybrid detection on identical held-out versions. |
| XTPU-0125 | blocked | medium | l | Visual/semantic image relatedness is blocked on a frozen retrieval corpus and metrics, signed CLIP-compatible ncnn artifact, embedding consumer, batching crossover measurements, and named-GPU acceptance. |
| XTPU-0126 | blocked | high | xl | PCB inspection is blocked on representative boards and missing/misplaced/rotated/damaged-component labels, a signed detector, camera/lighting fixture, and real-board acceptance; DeepPCB surface-defect pairs do not cover the requested component task. |
| XTPU-0127 | blocked | medium | l | Foreground/person segmentation is blocked on selecting a license-compatible model, a frozen labeled mask corpus and quality thresholds, a publisher signing identity, an exported qualified ncnn artifact, and named-Vulkan accuracy and performance evidence; this host provides suitable AMD Vulkan hardware but none of the model evidence. |
| XTPU-0128 | blocked | medium | m | Background removal is blocked on XTPU-0127's absent qualified mask producer and original-geometry contract; after that exists, measure host/GPU composition placement and verify atomic no-overwrite PNG/WebP publication against real masks. |
| XTPU-0049 | blocked | low | m | The profile now declares no model at all, because the runtime refuses one without a sha256 and the ncnn artifact has never been produced; restore the declaration from `omnitensor/model-recipes/retinexformer-lol-v1.json` together with the artifact and its digest. Then record reproducible `low-light-enhancement` 0.2.0 acceptance on the named RX 6600 XT: exact signed ncnn artifact/runtime identity, full Vulkan execution with no CPU fallback, portable/native and paired-image fidelity, warm p95, and measured end-to-end speedup over the documented CPU baseline. The profile remains disabled until evidence exists. |
| XTPU-0129 | blocked | high | xl | Low-light enhancement 0.2.0 is Vulkan/ncnn-only and returns enhanced RGB. Operational readiness remains blocked on the signed Retinexformer artifact, licensed paired corpus, completed pipeline, Cinnamon image-selection/run/progress/result UX, fidelity evidence, and named-GPU end-to-end acceptance. |
| XTPU-0130 | blocked | medium | xl | Ergonomic posture reminders are blocked because this host has no webcam; obtain consented representative posture data, a camera fixture, signed pose artifact, non-medical thresholds, privacy controls, and real acceptance. |
| XTPU-0131 | blocked | medium | l | Webcam auto-framing is blocked on the pose/person provider, absent webcam, camera-control adapter, smoothing/crop benchmarks, preview/confirmation UX, and real-device acceptance. |
| XTPU-0132 | blocked | high | xl | 3D-printer failure detection is blocked on a printer/camera fixture, printer-specific normal/failure corpus, signed model, continuous-monitoring consent/scheduling, and false-positive acceptance; model output must not stop a print. |
| XTPU-0133 | blocked | medium | l | Music genre, instrument, and mood tagging is blocked on separate bounded label ontologies, license-compatible timestamped audio corpora and held-out splits, qualified signed audio artifacts/tensor contracts, confidence calibration, and named-hardware evidence. Unblock by publishing those inputs and a timestamped evidence schema, then benchmarking deterministic features and host/GPU candidates for per-label precision/recall, calibration, latency, and peak memory on explicitly selected audio. |

## Rejected / Won't fix

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| XTPU-0087 | rejected | high | l | Reject an Electron/Node Windows tray: the portable client is Python under `../xpuwlm`, so JavaScript reuse no longer supports that design. Windows client ports remain tracked by XTPU-0203 and runtime transport by XTPU-0088. |
| XTPU-0177 | wont_fix | high | xs | Treat Sonar CSS rule S4654 against Cinnamon St's supported `spacing` property as false-positive; retain valid St layout declarations instead of rewriting them for browser CSS semantics. |
| XTPU-0178 | wont_fix | high | xs | Sonar S5443 reports `/tmp` strings that are inert test-fixture data and never reach a filesystem API — five of them today, in `tests/unit/xpuwlm-launcher.test.js:52-58` and `tests/unit/snapshot-reader.test.js:72`, where they are argument-quoting and path-expansion fixtures. S1523 reports the intentional CJS compatibility loader at `tests/cjs/production-smoke.js:54`, whose `new Function` compilation is restricted to the enumerated checked-in production sources. Retain these reviewed patterns and mark the Sonar issues false-positive when project administration is available. |
| XTPU-0179 | wont_fix | medium | xs | Retain global regular-expression replacement supported by the Cinnamon 6.0–6.6 floor in `panel-status.js` and `xpuwlm-launcher.js`; JSON-only contracts also intentionally reject values outside their transport shape. Mark corresponding Sonar S7781/S7784 reports false-positive. |
| XTPU-0180 | wont_fix | medium | xs | Treat Sonar S7924 reports for decorative `aria-hidden` SVG icons beside a labelled panel button as false-positive; retain the accessibility pattern. |
