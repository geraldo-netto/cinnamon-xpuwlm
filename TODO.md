# TODO

## Work groups

Items remain in the canonical lifecycle tables below; these ranges provide the
domain grouping without weakening the required status schema.

- **Project split and portability:** XTPU-0197–XTPU-0200, XTPU-0205, XTPU-0088
- **Hardware and storage:** XTPU-0104–XTPU-0110
- **Scheduling, network, and security:** XTPU-0111–XTPU-0114
- **Developer tools:** XTPU-0115–XTPU-0120
- **Local files and routines:** XTPU-0125
- **Vision and image processing:** XTPU-0049, XTPU-0126–XTPU-0129
- **Camera, equipment, and audio:** XTPU-0130–XTPU-0133
- **Wiring gaps:** XTPU-0183, XTPU-0185, XTPU-0186, XTPU-0188, XTPU-0189
- **Install and verification:** XTPU-0191
- **Ledger hygiene:** XTPU-0184
- **SonarCloud remediation:** XTPU-0177–XTPU-0180

## Findings

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| XTPU-0257 | open | low | xs | — | The payload's UUID is written out as a string literal in three JavaScript files — `applet.js`, `scripts/package-applet.js`, `scripts/validate-artifacts.js` — and a fourth time in `metadata.json`. The validator already requires the packaging module and already checks the metadata copy, so two of the three literals can be the one the packaging module exports. |
| XTPU-0258 | open | low | s | — | Three directory walks over the same tree, in two scripts: `payloadFiles` and `installedFiles` in `scripts/package-applet.js` and `payloadPaths` in `scripts/validate-artifacts.js`. They genuinely differ in policy — refuse an irregular entry, list it as a finding, or assert on it — and in whether directories are named, but the walk itself is one recursion written three times, and the sort order they must agree on is what a release's staging, manifest and archive all depend on. One walk that takes the policy as an argument. |
| XTPU-0259 | open | low | s | — | `applet.js` carries the panel icon's whole presentation — the size floor and its panel-height bound, the status style class swap, the symbolic name, the inline `icon-size` override, and registering and releasing the payload's icon search path — mixed in among settings binding, menu construction, the refresh timer and teardown. It is one coherent subject with its own rules and its own failure (an icon larger than the strip it draws in), and it is the reason the file is 512 lines. Split it into a `lib/` module the applet holds, keeping the drawn result identical. |
| XTPU-0260 | open | low | xs | — | Every user-visible string in the helper lives in `lib/panel-status.js` — deliberately, since that module owns the wording, the interpolation and the plural rule — except two: the title and body of the launch-failure notification, written out in `applet.js` `_launch`. A person reading the module that owns the panel's words cannot see one of the sentences the panel says. |
| XTPU-0261 | open | low | xs | — | `validateStylesheet` proves the stylesheet balances by counting `{` and `}` across the whole file, comments included. The shipped sheet opens with an eight-line comment; a brace written into that comment, or into any future one, either satisfies the count while the rules are unbalanced or fails it while they are not. Count braces in the declarations, not in the prose. |
| XTPU-0262 | open | low | xs | — | `stageSpiceRelease` checks the four Spice metadata files with `regularFile` and then checks them again: `inspectSpiceSources`, its own first statement, has already run the same guard over the same list. It also calls `fs.mkdirSync(targetRoot, {recursive: true})` once per file inside the copy loop rather than once before it, and discards the `{info, screenshot}` `inspectSpiceSources` hands back — which the validator does read, so the return value is neither unused nor honoured consistently. |

## Blocked

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| XTPU-0191 | blocked | low | s | — | Live verification polls blind for its whole budget. The D-Bus premise this row was written from is gone — session D-Bus was removed on both sides, and `../omnitensor/src/omnitensor/acceptance_cli.py` probes through `SocketApplyCommandProbe`, which retries `apply-command` over the control socket every 250 ms against a 30 s deadline (`acceptance_probes.py:71-111`). The cost survives the change of transport: `omnitensor-verify-install` run immediately after `systemctl --user restart` still spends the full budget before reporting, because nothing tells it when the service is ready. The fix is to wait on a readiness signal — the socket file appearing under the service's `RuntimeDirectory`, or systemd's own notify. Blocked here rather than open: the prober, the CLI and the unit file are all in `../omnitensor`, a separate repository this ledger's repository does not write to, and this applet has no part in the verification path — it never opens the control socket. Settle it there and delete this row. |
| XTPU-0198 | blocked | medium | m | XTPU-0197, XTPU-0250, XTPU-0251 | Package and install the three pieces per platform. The Cinnamon half of this row is done and is not waiting on anything: `package-applet.js` stages a checksummed payload, `docs/deployment.md#installing-the-applet` is the install sequence, and `docs/deployment.md#install-order` now states the ordering constraint the pinned `SNAPSHOT_VERSION` demands — reader before writer — and what a mismatch looks like in the popup. What remains is unpacked into XTPU-0250 and XTPU-0251, and both live outside this repository: the Python client's per-platform install is `../xpuwlm`'s, and install-time verification spans the client and `../omnitensor`. Blocked here for that reason; hand the two sub-rows over or settle them there. |
| XTPU-0250 | blocked | medium | s | XTPU-0198 | Write the Python client's per-platform install sequence. `../xpuwlm/pyproject.toml` is a hatchling wheel with an `xpuwlm` console script and `omnitensor>=0.1,<1` as its only required dependency, so the artifact exists and only the sequence is missing. Blocked: the document belongs beside the client, in `../xpuwlm`. |
| XTPU-0251 | blocked | medium | xs | XTPU-0198, XTPU-0250 | Give the install an active verification step that names a version mismatch between client, service and panel at install time rather than leaving a desktop that silently shows nothing. The panel's own half already reports (`docs/deployment.md#install-order`); the missing half is a check the installer runs. Blocked: it spans `../xpuwlm` and `../omnitensor`. |
| XTPU-0197 | blocked | high | m | XTPU-0245, XTPU-0246, XTPU-0247, XTPU-0248, XTPU-0249, XTPU-0203, XTPU-0204 | Put the Python client's platform ports behind interfaces; four of the eight named ports do not exist at all. Verified on 2026-08-17: the transport is borrowed whole from the service distribution (`runtime/client.py` calls `omnitensor.socket_transport.call_control`, framed msgpack over the unix socket, liveness from the socket file under the service's `RuntimeDirectory`); the file chooser is a bare `Gtk.FileChooserDialog` at `ui/fields.py:177`; the state and settings paths live in `runtime/settings.py`. Clipboard reading, screenshot capture, notifications and autostart have no implementation and no caller, and nothing is behind a `Protocol` or `ABC`, so every one of these is a direct Linux call at its use site. Unpacked into XTPU-0245–XTPU-0249 so the cost is visible. Blocked here rather than open: every file this row edits lives in `../xpuwlm`, a separate repository this ledger's repository does not write to. Settle it there and delete this row, or hand the sub-rows over with it. |
| XTPU-0245 | blocked | high | s | XTPU-0197 | Name the eight platform ports of the Python client as interfaces — transport, file chooser, state path, settings path, clipboard, screenshot, notifications, autostart — as `Protocol` declarations in one module, with no implementation attached. Blocked with its parent: the files are in `../xpuwlm`. |
| XTPU-0246 | blocked | high | s | XTPU-0197, XTPU-0245 | Move the four working Linux call sites behind the interfaces XTPU-0245 declares — the msgpack socket transport in `runtime/client.py`, the `Gtk.FileChooserDialog` at `ui/fields.py:177`, and the state and settings paths in `runtime/settings.py` — keeping the shipped Linux behaviour byte for byte and changing only where it is named from. Blocked with its parent: the files are in `../xpuwlm`. |
| XTPU-0247 | blocked | high | s | XTPU-0197, XTPU-0245 | Write the Linux implementations of the two capture ports that have none — clipboard reading and screenshot capture — behind the XTPU-0245 interfaces. Screenshot capture is also the source XTPU-0188 needs. Blocked with its parent: the files are in `../xpuwlm`. |
| XTPU-0248 | blocked | medium | s | XTPU-0197, XTPU-0245 | Write the Linux implementations of the two session ports that have none — desktop notifications and autostart registration — behind the XTPU-0245 interfaces. Blocked with its parent: the files are in `../xpuwlm`. |
| XTPU-0249 | blocked | medium | xs | XTPU-0197, XTPU-0245 | Make a port a platform genuinely lacks refuse deliberately rather than no-op silently: one unsupported-port error raised from the interface, so a missing capability is reported to the caller instead of appearing to have worked. Blocked with its parent: the files are in `../xpuwlm`. |
| XTPU-0222 | blocked | low | xs | — | Verify the panel-height-bounded icon floor on a desk. The code half is done: `panelIconSize` (`applet.js`) takes the panel height and bounds `MIN_PANEL_ICON_SIZE` by it, so a 20-pixel panel is never asked to draw a 28-pixel icon through the inline `icon-size` style the theme cannot outrank, and the applet keeps `_panelHeight` current from Cinnamon's own Applet. `tests/fuzz/icon-artifact.fuzz.test.js` already holds every geometry bounded across panel sizes, so what remains is not code but eyes: run the applet at panel heights 20, 28 and 40 and confirm the glyph sits inside the strip at each, and that a height change redraws at the new bound. Blocked on the maintainer: this needs a live Cinnamon session on a physical desk, which no automated run in this repository has. |
| XTPU-0244 | blocked | low | xs | — | Decide whether a snapshot with no usable timestamp can be reported as connected. `stateFromDocument` reads `generatedAt` only when it is an integer and `isStale` skips the check when it is null, which `tests/unit/snapshot-reader.test.js` pins deliberately ("a snapshot with no timestamp is read rather than judged stale"). The consequence is that a file left behind by a runtime that stopped — one written once, or written with a timestamp the panel cannot read — is drawn as a live runtime for the rest of the session, which is the one picture staleness exists to prevent. The canonical schema declares `generatedAt`, so the alternative is to treat its absence as a document this panel cannot judge and say so. Blocked on the maintainer: is a snapshot whose freshness cannot be established still `connected`, or is it `stale` (or `malformed`)? Either answer changes shipped behaviour and the pinned test. |
| XTPU-0230 | blocked | low | xs | XTPU-0180 | Four of the nine shipped SVGs — `xpuwlm-symbolic.svg`, `xpuwlm-v2-symbolic.svg`, `xpuwlm-device-symbolic.svg`, `xpuwlm-sliders-symbolic.svg` — are named by no production code: the applet only ever asks for `xpuwlm-status-*-symbolic` (`panel-status.js`) and `system-run-symbolic`, and `metadata.json` declares no icon. They are staged into the payload and their directory is appended to the icon theme search path regardless. Only the tests and the validator name them. Blocked on a maintainer decision: are these kept deliberately as the applet's identity artwork for a Spice listing or a future surface — in which case something should reference them and say so — or should the payload ship only the glyphs it draws? Deleting shipped artwork is not a call to make from a rescan. |
| XTPU-0200 | blocked | medium | s | XTPU-0186, XTPU-0188, XTPU-0189 | Re-decide the three surviving unwired-module questions now that the split is real and the modules are gone. One of the four is settled and its row is retired: `runtime-control-service` was deleted with the helper and the runtime is the only policy authority, which `../xpuwlm`'s D rows and `../omnitensor`'s OMNI-0357 now both record. The other three moved rather than closed, because the product documents that describe them moved to the client repository with `962a317` — `../xpuwlm/docs/screenshot-assistant.md`, `routine-recognition.md`, `caption-export.md`, `presentation-*.md`, `rehearsal-briefing.md`. So XTPU-0186, XTPU-0188 and XTPU-0189 are questions for the Python client, which has the host runtime GJS never had. Settle them there and record the outcome here, or hand the rows over and delete them from this ledger — that hand-over decision is what this row is now asking for. Blocked on the maintainer: do XTPU-0186, XTPU-0188 and XTPU-0189 stay in this ledger as questions for the Python client, or are they handed over to `../xpuwlm` and deleted here? An audit cannot make that hand-over on its own. |
| XTPU-0183 | blocked | medium | l | XTPU-0200, XTPU-0185, XTPU-0186, XTPU-0188, XTPU-0189 | Decide which of the never-wired libraries the Python client should carry, and wire the ones that survive. The GJS half is closed and verified: the helper is `applet.js` plus five `lib/` modules, and `tests/contract/production-payload-contract.test.js` fails if any JavaScript file in the payload is unreachable from the applet, so an unwired module cannot reappear. The fourteen modules (`artifact-qualification`, `caption-export`, `deterministic-action-port`, `file-auto-tagging`, `file-categorization`, `image-duplicate-benchmark`, `presentation-planning`, `presentation-review`, `readiness-acceptance`, `rehearsal-briefing`, `routine-recognition`, `runtime-control-service`, `screenshot-assistant`, `workload-benchmark`) live only in this repository's git history. What remains is the product question each one asked, held by the four decision rows above; `runtime-control-service` is settled by deletion, the runtime being the only policy authority. Blocked on the four decision rows below it; nothing here is implementable until each names a product answer. |
| XTPU-0185 | blocked | medium | l | XTPU-0183, XTPU-0200 | Decide where the deterministic write path confirms, before anything wires the caption and presentation chain. The GJS modules this row named are deleted (`537220a`) and the product documents moved to the client with `962a317`, so the decision is now the Python client's: `caption-export` turns a completed media transcription into SRT/WebVTT and `presentation-planning` exports a deck, and both reach the user's filesystem — preview, conflict check, audit evidence, and a fresh single-use confirmation before the writer runs. Neither surface exists in `../xpuwlm` today. Decide whether the confirmation lives in the client's workload window, a modal, or the runtime, then chain `caption-export` behind the completed media-transcription result, and `presentation-review` → `presentation-planning` → `rehearsal-briefing` behind a presentation workload. Nothing can be wired until `../omnitensor/plugin-manifests/` declares these workloads; none does today. Blocked on the maintainer: where does the deterministic write path confirm — the client workload window, a modal, or the runtime — and does `../omnitensor/plugin-manifests/` declare the caption and presentation workloads first? |
| XTPU-0186 | blocked | medium | m | XTPU-0183, XTPU-0200 | Decide where `artifact-qualification` and `readiness-acceptance` get their documents. The GJS validators are deleted, so the question is what the runtime should publish rather than what the applet should keep: `describe-plugins` carries `{id, version, format, ready, reason}` per artifact and no qualification envelope, and no control method publishes readiness evidence at all. Either extend the runtime's inventory reply to carry them — a coordinated two-repository contract change — or accept that these validate build-time artifacts only and belong in `../omnitensor/scripts/`, next to the acceptance tooling that already produces the evidence, rather than in any shipped client. Blocked on the maintainer: does the runtime extend `describe-plugins` to carry qualification and readiness evidence (a two-repository contract change), or do these validators move to `../omnitensor/scripts/` as build-time tooling? |
| XTPU-0188 | blocked | medium | l | XTPU-0183, XTPU-0200 | Give `screenshot-assistant` and `routine-recognition` the sources neither client collects. The screenshot assistant accepts one explicitly captured screenshot or selected image and submits it to the media vision worker; `../xpuwlm` has a file chooser (`ui/fields.py:177`) but no screenshot capture at all, which is also the missing port XTPU-0197 tracks. Routine recognition accepts bounded, content-free activity summaries — time buckets, weekday, categorical application and device classes — which nothing produces on either side; it needs a consented collector with the runtime telemetry recorder's redaction discipline, and its consent is a separate decision from the load record's. Neither workload is declared in `../omnitensor/plugin-manifests/` today, so the runtime half is a prerequisite. Blocked on the maintainer: screenshot capture and a consented activity collector are both new data sources with their own consent decisions, and neither workload is declared in `../omnitensor/plugin-manifests/`. |
| XTPU-0189 | blocked | medium | l | XTPU-0183, XTPU-0200 | Decide what the file workflows compare before anything wires them. `file-categorization`, `image-duplicate-benchmark` and the shared `file-auto-tagging` were never classifiers — they were benchmark harnesses that measure candidate implementations against each other and recommend one, and refused fewer than two candidates. Their GJS implementations are deleted, so wiring them means writing two real taggers in the Python client, not restoring a controller: a deterministic suffix-based host candidate, and a GPU candidate through the runtime. The runtime side is reachable — the external workers start and the installed tools read Ready — so the blocker is meaning rather than availability: choosing what tagging does and against which taxonomy. Note `../omnitensor/workloads/` declares none of these three, so a runtime candidate has nothing to call yet. Blocked on the maintainer: what does tagging mean and against which taxonomy? Two real taggers have to be specified before either can be written, and `../omnitensor/workloads/` declares none of the three. |
| XTPU-0184 | blocked | low | s | — | Decide what `related ids` means once a referenced row is removed, then repair the ledger. Recounted after this audit: 26 of 48 rows cite at least one of `XTPU-0095`–`XTPU-0103`, none of which has a row, and the blocked hardware and developer-tool rows carry almost all of those citations — `XTPU-0095` alone appears in 24 of them. Cross-repository `OMNI-####` references are deliberate and are not affected. Either restore the prerequisite rows the dependencies point at, or rewrite each dependency as prose in its description — and record the rule, so the next removal does not re-create the problem. Blocked on the maintainer: does `related ids` mean a live row only — in which case the 26 rows citing XTPU-0095–XTPU-0103 have their dependencies rewritten as prose — or are those prerequisite rows restored? Repairing it either way edits blocked rows, which an audit does not touch. |
| XTPU-0218 | blocked | low | xs | XTPU-0207 | Decide the panel's poll cadence against the cadence the service actually publishes at. Revealed by resolving XTPU-0207: the shipped default is one second, while `../omnitensor/src/omnitensor/service.py:135-146` publishes every two seconds under load and every ten when idle, with a ten-second proof-of-life heartbeat. So at the default the panel performs one blocking read and JSON parse per second to observe a document that changes at most half that often, and a tenth as often on an idle desk. Options: (a) leave one second and accept the reads, (b) follow the service's two-second publish interval, (c) track it — poll fast only while the last snapshot said something was running. (b) and (c) change shipped behaviour and the regression test that pins it (`tests/regression/refresh-interval-default-regression.test.js`), so the choice is the maintainer's. |
| XTPU-0219 | blocked | low | xs | — | Decide whether four shipped icons are payload or dead weight. `icons/xpuwlm-symbolic.svg`, `xpuwlm-v2-symbolic.svg`, `xpuwlm-device-symbolic.svg` and `xpuwlm-sliders-symbolic.svg` are asserted by `scripts/validate-artifacts.js:268-278`, rendered by the fuzz and visual gates, and referenced by no code: the applet draws only the five `xpuwlm-status-*` names, and the deleted screens are what used the other four. Options: (a) keep them as the project's identity and Spice listing artwork and say so in `docs/applet.md`, (b) keep only `xpuwlm-symbolic.svg` for that purpose and delete the other three, (c) delete all four and let `icon.png` carry the identity. Deleting shipped payload changes what an installed applet contains and what the Spice listing can show, which is the maintainer's call, not an audit's. |
| XTPU-0088 | blocked | high | xl | XTPU-0197, XTPU-0203 | Full Windows inference parity is blocked on a compatible OmniTensor Windows service: the current control plane is a Linux unix-domain socket, fallback discovery reads `/dev` and `/sys`, and accelerator SDK/driver support must be proven. The transport half is decided and shipped on Linux — framed msgpack over a unix socket, a per-user named pipe being the Windows equivalent — which leaves this row blocked on the genuinely Windows-shaped work: service-authoritative device discovery to replace the `/dev` and `/sys` reads, accelerator SDK/driver proof, and a Windows host to develop against before the Python client in `../xpuwlm` (XTPU-0203) can provide full functionality there. |
| XTPU-0203 | blocked | high | l | XTPU-0197, XTPU-0088 | Windows implementations of the client's platform ports. The client itself now exists (`../xpuwlm`), so this is no longer waiting on a codebase — but it is still blocked twice over: no Windows host exists here to develop or verify against, and the runtime side has no Windows service or per-user IPC transport (XTPU-0088), so a Windows client would have nothing to talk to. The ports it would implement are not yet interfaces either (XTPU-0197). Unblock with a Windows development host plus the runtime transport work. |
| XTPU-0204 | blocked | medium | l | XTPU-0197 | macOS implementations of the client's platform ports. — blocked: no macOS host exists here to develop or verify against, and the runtime has never been built or probed on macOS (device discovery reads `/dev/dri`, `/dev/accel`, `/dev/apex_*`). Unblock with a macOS development host and a runtime discovery story for Metal-class hardware. |
| XTPU-0104 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0098, XTPU-0099, XTPU-0101, OMNI-0085, OMNI-0086, OMNI-0087 | Fan degradation prediction is blocked on representative failing/normal fan data, device-specific RPM/PWM/load telemetry, an acoustic or vibration fixture if those signals are claimed, a signed model, and real failure acceptance; NASA aircraft-engine simulation data cannot validate desktop fans. |
| XTPU-0105 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0085, OMNI-0086, OMNI-0087 | Power-supply health scoring is blocked because this host exposes no trustworthy PSU voltage/current/temperature/fan telemetry; obtain supported PMBus/BMC/vendor hardware, labeled failure data, and a signed qualified model. |
| XTPU-0106 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0085, OMNI-0086, OMNI-0087 | UPS battery degradation is blocked because no UPS/power-supply device or Network UPS Tools client exists here; obtain supported hardware, discharge/load histories, failure labels, and a signed qualified model while preserving deterministic shutdown policy. |
| XTPU-0107 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0088, OMNI-0089, OMNI-0090 | Disk/SSD failure warning is blocked on permission to read all seven discovered SMART/NVMe devices, representative local SSD/HDD histories and outcomes, device-family validation, signing identity, and real-drive false-positive/recovery acceptance. |
| XTPU-0108 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0085, OMNI-0086, OMNI-0087 | Memory-fault precursor detection is blocked because EDAC reports no memory controller on this host and representative corrected-error/failure sequences are absent; obtain a supported ECC/RAS host, labels, signed model, and real acceptance. |
| XTPU-0109 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0103, XTPU-0107, OMNI-0088, OMNI-0089, OMNI-0090 | Storage-tier recommendation is blocked on the storage telemetry/model pipeline, representative access/retention histories, measured utility, and safe deterministic migration/rollback adapters; model output must remain advisory. |
| XTPU-0110 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0091, OMNI-0092, OMNI-0093 | Workload placement hints are blocked on representative local job traces and target utility/fairness labels, a signed qualified model, deterministic starvation policy, and named-host acceptance; public cluster traces are prototype data only. |
| XTPU-0111 | blocked | medium | l | XTPU-0095, XTPU-0099, XTPU-0100, OMNI-0091, OMNI-0092, OMNI-0093 | Queue-delay forecasting is blocked because the runtime has no labeled representative workload traces, qualified signed `resource-scheduler` model/tensor contract, bounded advisory pipeline, or named-hardware accuracy, calibration, latency, and fairness evidence. Unblock by publishing those runtime artifacts, freezing a local trace corpus with target delays, and benchmarking deterministic host, GPU, and hybrid paths before exposing a typed advisory through the generic Cinnamon surface. |
| XTPU-0112 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0098, XTPU-0101, OMNI-0108, OMNI-0109, OMNI-0110, OMNI-0111, OMNI-0184 | Port-scan/reconnaissance detection is blocked on a least-privilege flow collector, BPF access, privacy-reviewed current traffic labels, signed model, false-positive/attack-recall gates, and real network acceptance. |
| XTPU-0113 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0101, OMNI-0108, OMNI-0109, OMNI-0110, OMNI-0111 | Wi-Fi interference classification is blocked because Wi-Fi is disconnected and `iw` is absent; obtain survey/retry/channel telemetry, controlled congestion/interference/hardware-fault labels, a signed model, and real radio acceptance. |
| XTPU-0114 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0098, XTPU-0101, XTPU-0103, OMNI-0184 | Privilege-escalation precursor detection is blocked on approval of a separate host-security profile, missing audit tools and BPF privilege, a least-privilege redacted event helper, representative attack/normal sequences, and review-only acceptance. |
| XTPU-0115 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099, OMNI-0094, OMNI-0095, OMNI-0096 | Test prioritization experiments are blocked on a provenance-safe labeled change/failure/duration/ownership/coverage corpus, a qualified signed `build-advisor` model/tensor contract, its ranking pipeline, and named-hardware acceptance. Unblock by freezing the corpus, mandatory-test allowlist, time-to-first-failure and recall-at-budget metrics, publishing the runtime artifacts, and benchmarking deterministic, host, GPU, and hybrid rankers without omitting required checks. |
| XTPU-0116 | blocked | medium | l | XTPU-0095, XTPU-0099, XTPU-0101 | API-misuse classification is blocked because no license-compatible labeled corpus, frozen misuse taxonomy/splits, qualified signed model, or acceptance thresholds exist. Unblock by selecting one language/API corpus with gold labels, defining precision, recall, false-positive, latency, memory, and explanation-quality gates, then benchmarking deterministic AST/rules, host, GPU, and hybrid candidates on identical held-out data and named hardware. |
| XTPU-0117 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099 | Fuzz-input prioritization is blocked on a selected native fuzzer/target set, reproducible seeded corpora, instrumented path and held-out bug ground truth, an energy measurement method, and any qualified signed learned scorer. Unblock by freezing those fixtures and paths/bugs-per-wall-clock-and-joule metrics, then running repeated native-selection, host, GPU, and hybrid campaigns with identical budgets on named hardware. |
| XTPU-0118 | blocked | medium | l | XTPU-0095, XTPU-0099 | Fuzz-crash deduplication is blocked on a license-compatible crash corpus with reviewed bug-equivalence clusters across stack/sanitizer variants, a frozen normalization baseline, qualified signed embedding artifacts, and an operator-cost protocol. Unblock by publishing those fixtures and pairwise precision/recall, latency, peak-memory, and review-time metrics, then benchmarking normalized signatures, host clustering, GPU embeddings, and hybrid cascades on identical named hardware. |
| XTPU-0119 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0103 | Compiler-option suggestion is blocked on pinned compiler/toolchain targets, validated allowlisted profiles, a reproducible labeled build corpus, correctness oracles, and any qualified signed advisor. Unblock by freezing those inputs and deterministic refusal policy, then comparing search, host, GPU, and hybrid recommenders on identical builds for correctness, build cost, output size, runtime, and peak memory without generating arbitrary flags. |
| XTPU-0120 | blocked | medium | l | XTPU-0095, XTPU-0096, XTPU-0099, OMNI-0094, OMNI-0095, OMNI-0096 | Resource-regression experiments are blocked on versioned known-good/regressed builds, controlled repeated CPU, memory, I/O, GPU, and startup measurements, noise budgets and labels, plus qualified signed `build-advisor` artifacts and acceptance. Unblock by freezing the workload/host harness and false-positive/detection gates, publishing the runtime model and pipeline, and comparing statistical, host-model, GPU-model, and hybrid detection on identical held-out versions. |
| XTPU-0125 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099, XTPU-0100, XTPU-0101, OMNI-0097, OMNI-0098, OMNI-0101 | Visual/semantic image relatedness is blocked on a frozen retrieval corpus and metrics, signed CLIP-compatible ncnn artifact, embedding consumer, batching crossover measurements, and named-GPU acceptance. |
| XTPU-0126 | blocked | high | xl | XTPU-0095, XTPU-0097, XTPU-0099, XTPU-0101 | PCB inspection is blocked on representative boards and missing/misplaced/rotated/damaged-component labels, a signed detector, camera/lighting fixture, and real-board acceptance; DeepPCB surface-defect pairs do not cover the requested component task. |
| XTPU-0127 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099, XTPU-0102 | Foreground/person segmentation is blocked on selecting a license-compatible model, a frozen labeled mask corpus and quality thresholds, a publisher signing identity, an exported qualified ncnn artifact, and named-Vulkan accuracy and performance evidence; this host provides suitable AMD Vulkan hardware but none of the model evidence. |
| XTPU-0128 | blocked | medium | m | XTPU-0100, XTPU-0102, XTPU-0103, XTPU-0127 | Background removal is blocked on XTPU-0127's absent qualified mask producer and original-geometry contract; after that exists, measure host/GPU composition placement and verify atomic no-overwrite PNG/WebP publication against real masks. |
| XTPU-0049 | blocked | low | m | XTPU-0129, OMNI-0105, OMNI-0106 | The profile now declares no model at all, because the runtime refuses one without a sha256 and the ncnn artifact has never been produced; restore the declaration from `omnitensor/model-recipes/retinexformer-lol-v1.json` together with the artifact and its digest. Then record reproducible `low-light-enhancement` 0.2.0 acceptance on the named RX 6600 XT: exact signed ncnn artifact/runtime identity, full Vulkan execution with no CPU fallback, portable/native and paired-image fidelity, warm p95, and measured end-to-end speedup over the documented CPU baseline. The profile remains disabled until evidence exists. |
| XTPU-0129 | blocked | high | xl | XTPU-0049, XTPU-0095, XTPU-0097, XTPU-0101, OMNI-0048, OMNI-0102, OMNI-0103, OMNI-0104, OMNI-0105, OMNI-0106 | Low-light enhancement 0.2.0 is Vulkan/ncnn-only and returns enhanced RGB. Operational readiness remains blocked on the signed Retinexformer artifact, licensed paired corpus, completed pipeline, Cinnamon image-selection/run/progress/result UX, fidelity evidence, and named-GPU end-to-end acceptance. |
| XTPU-0130 | blocked | medium | xl | XTPU-0095, XTPU-0097, XTPU-0098, XTPU-0100, XTPU-0101 | Ergonomic posture reminders are blocked because this host has no webcam; obtain consented representative posture data, a camera fixture, signed pose artifact, non-medical thresholds, privacy controls, and real acceptance. |
| XTPU-0131 | blocked | medium | l | XTPU-0100, XTPU-0101, XTPU-0103, XTPU-0130 | Webcam auto-framing is blocked on the pose/person provider, absent webcam, camera-control adapter, smoothing/crop benchmarks, preview/confirmation UX, and real-device acceptance. |
| XTPU-0132 | blocked | high | xl | XTPU-0095, XTPU-0096, XTPU-0097, XTPU-0098, XTPU-0101 | 3D-printer failure detection is blocked on a printer/camera fixture, printer-specific normal/failure corpus, signed model, continuous-monitoring consent/scheduling, and false-positive acceptance; model output must not stop a print. |
| XTPU-0133 | blocked | medium | l | XTPU-0095, XTPU-0097, XTPU-0099, XTPU-0102 | Music genre, instrument, and mood tagging is blocked on separate bounded label ontologies, license-compatible timestamped audio corpora and held-out splits, qualified signed audio artifacts/tensor contracts, confidence calibration, and named-hardware evidence. Unblock by publishing those inputs and a timestamped evidence schema, then benchmarking deterministic features and host/GPU candidates for per-label precision/recall, calibration, latency, and peak memory on explicitly selected audio. |

## Rejected / Won't fix

| id | status | severity | effort | related ids | description |
| --- | --- | --- | --- | --- | --- |
| XTPU-0087 | rejected | high | l | XTPU-0203 | An Electron/Node tray spike was the recorded Windows path, premised on reusing the applet's JavaScript library surface. The split (XTPU-0196, and the client rows XTPU-0197–XTPU-0205) superseded that premise, and the split has since happened: the portable client is Python and lives in `../xpuwlm`, so the JavaScript-reuse argument and the Electron spike no longer describe anything that could be built. Windows client work now lives in XTPU-0203 (platform ports) and the runtime-side transport it needs stays recorded in XTPU-0088. Kept as a tombstone so a later audit does not re-propose the Electron path. |
| XTPU-0177 | wont_fix | high | xs | — | Sonar CSS rule S4654 reported `spacing` as an unknown browser property, against a stylesheet targeting Cinnamon's St theme engine where `spacing` is a supported layout property. The count went 24 → 1 → 0: the 900-line sheet became 37 lines with one `spacing` declaration (`.xpuwlm-status-line`), and XTPU-0217 deleted that rule as dead — no actor was ever given the class. The shipped sheet now declares only the five `xpuwlm-panel-*` status colours, so the rule has nothing left to report. Kept as a tombstone: St layout properties are legitimate here, and a future `spacing` is to be retained and marked false-positive rather than rewritten to satisfy a browser rule. |
| XTPU-0178 | wont_fix | high | xs | — | Sonar S5443 reports `/tmp` strings that are inert test-fixture data and never reach a filesystem API — five of them today, in `tests/unit/xpuwlm-launcher.test.js:52-58` and `tests/unit/snapshot-reader.test.js:72`, where they are argument-quoting and path-expansion fixtures. S1523 reports the intentional CJS compatibility loader at `tests/cjs/production-smoke.js:23`, whose `new Function` compilation is restricted to the enumerated checked-in production sources. Retain these reviewed patterns and mark the Sonar issues false-positive when project administration is available. |
| XTPU-0179 | wont_fix | medium | xs | — | Retain the reviewed JavaScript compatibility patterns behind Sonar S7781 and S7784: global regular-expression replacement is supported across the Cinnamon 6.0–6.6 floor and is still used in `lib/i18n.js` (`format`) and `lib/xpuwlm-launcher.js` (`quoteArgument`), and the JSON-only contracts intentionally reject values `structuredClone` cannot carry. The other two halves of this row died with the shrink and are not to be re-raised as live: no `structuredClone` call, no per-instance cancellation state (S2094's constructor assignments), and no workflow class of any kind survives in the helper. Mark whatever the next scan still reports won't-fix. |
| XTPU-0180 | wont_fix | medium | xs | — | Sonar S7924 treats `aria-hidden` SVG icons as text; there are nine such icons under `files/cinnamon-xpuwlm@geraldo-netto/icons/`, all decorative beside a labelled panel button. The 16 translucent prototype backgrounds this row also covered are no longer scanned from here — the prototype moved to `../xpuwlm/design/prototype/` with `5f70af6`/`962a317` — and the primary-button gradient finding was applicable and was fixed. Retain the icon rules and mark them false-positive after the next scan. |
